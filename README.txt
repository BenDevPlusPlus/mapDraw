README

This small project is an experiment in drawing on a canvas, eventually to use it to draw plans, boxes and labels onto an existing map.

Eventually I want to make each box a selectable, rotatable and draggable object.